<h1>Preguntar</h1>

<% if @question.errors.any? %>
  <ul>
    <% @question.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= form_for @question do |f| %>
  <div>
    <%= f.text_field :name, placeholder: "Preguntar" %>
  </div>
  <div>
    <%= f.text_area :description, placeholder: "Descripción" %>
  </div>
  <div>
    <%= f.submit "Preguntar" %>
  </div>
<% end %>
